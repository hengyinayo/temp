spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:8100/ybigta_test?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&useSSL=false
    username: root
    password: root
    hikari:
      idle-timeout: 500
      connection-timeout: 15000
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
